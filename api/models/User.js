module.exports = {
  attributes: {
    // e.g., "Polly"
    name: {
      type: 'string',
      required: true
    },

    // e.g., mail@mail.com
    email: {
      type: 'string'
    },

    // e.g., "Beograd"
    city: {
      type: 'string'
    },

    // e.g., Srbija
    country: {
      type: 'string'
    }
  }
}